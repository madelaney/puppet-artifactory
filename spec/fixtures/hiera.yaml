---
version: 5

defaults:
  datadir: 'hieradata'
  data_hash: 'yaml_data'

hierarchy:
  - name: 'Operating System Family'
    path: "%{facts.os.kernel}.yaml"

  - name: 'default values'
    path: 'default.yaml'
